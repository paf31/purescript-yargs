{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp test -- -w hello -w world"
  },
  "dependencies": {
    "yargs": "^8.0.2"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript-psa": "^0.5.0",
    "purescript": "^0.12.0",
    "rimraf": "^2.5.4"
  }
}
